SWC -> Speedy Web Compiler, remplaza la utilizacion de Babel. Nos permite construir, compilar mas rapido.

En la carpeta public,van los recursos estaticos , como imagenes

Nota de la clase 39 -. Es recomendable declarar variables constantes fuera de los componentes de React, para que no formen parte del ciclo de este mismo.

Se utiliza Testing Library, para el testeo de componentes:
npm install --save-dev @testing-library/react @testing-library/dom
